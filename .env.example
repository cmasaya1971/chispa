# Copiá este archivo como `.env` y pegá tu API key de OpenAI.
# El archivo `.env` está en .gitignore: NUNCA se sube al repo.
OPENAI_API_KEY=sk-...

# Modelo por defecto (opcional). Sugerido: gpt-4o
OPENAI_MODEL=gpt-4o
